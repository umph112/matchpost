# VS Code 프롬프트 3 (봇) — 승인 답변

계획 보고에 대한 승인·정정입니다.

> ⚠️ 이 폴더의 문서는 전달받은 원본이 인코딩이 깨진 채로 도착해서 내용 기준으로 옮겨 적은 것입니다.

---

```
계획 정확해. 어긋난 4건 다 맞아 — 특히 「이메일 인증 우회가 필요 없다」와
「인플루언서는 approved, 광고주만 pending」은 내 문서가 몰랐던 거야.

[승인 1] Playwright 설치 → 진행해
  @playwright/test + playwright.config.ts + npm run test:e2e.

  ⚠️ headless: false 로 해줘. 크롬 창이 실제로 열려야 해.
     「막다른 길」과 「직관성」은 로그로 안 보여 —
     로그엔 「버튼 클릭 성공」인데 화면엔 아무 일도 안 일어난 경우가 그것이야.

  ⚠️ 각 단계마다 스크린샷을 남겨줘 (tests/screenshots/S0-1.png 처럼).
     내가 그걸 보고 화면 문제를 짚을게.

[승인 2] bot+admin@ 신규 관리자 → 진행해
  판단이 맞아. 두 지시가 부딪히는 걸 정확히 봤어.
  「기존 관리자를 건드리지 마」가 목적이었으니 새로 만드는 게 그 목적을 지켜.
  기존 관리자로는 로그인조차 하지 마.

[답변 3] 알림톡 → 가입에는 없는 게 맞아
  내가 말한 두 경로는 정산·협업 알림이야. 가입 경로엔 없어. 그 지시는 무시해줘.

[답변 4] 미수 만들기 → 결제일을 과거로 넣는 건 허용이야
  내가 금지한 건 「상태 컬럼을 직접 바꾸는 것」이야.
  결제 예정일은 화면(딜시트·결제일 변경 제안)으로 도달할 수 있는 값이니 괜찮아.
  화면으로 넣을 수 있으면 화면으로, 안 되면 값 조작도 허용해.

  ⚠️ 다만 settlement_status 나 paid_confirmed_at 은 직접 쓰지는 마.
     그건 화면으로만 도달해야 해 — 직접 쓰면 「화면으로는 만들 수 없는 상태」가 생겨.

[답변 5] 로컬에 돌려 → localhost:3000 맞아
  배포본에 돌리면 실제 DB에 봇 계정이 쌓여.
  단, 배포본에서만 도는 문제가 있을 수 있으니(환경변수·크론) 로컬에서 다 통과한 뒤
  S0 만 배포본에서 한 번 더 돌려줘.

[픽스처] 더미 PNG 만들어 넣어줘. tests/fixtures/biz-doc.png.

[순서] 3번(00-signup.spec.ts)부터 진행하고 로컬 1회 완주 결과를 보고해줘.
  ⚠️ S0 이 통과하기 전에 나머지 3개 파일은 쓰지 마.
     가입이 막히면 뒤가 전부 거짓 통과야.

[보고서 넣을 것]
  각 단계: 통과/막힘 · 막힌 이유 · 사람도 같은 곳에서 막히는지
  따로 모아서: [막다른 길] · [직관성]
  스크린샷 경로

봇이 막히면 우회하지 마. 거기서 멈춰서 무엇이 막았는지 적어줘.
```
