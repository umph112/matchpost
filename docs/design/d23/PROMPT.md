# VS Code 프롬프트 (D23) — 봇 시나리오

D21 · D22 가 끝난 다음에 전달하세요. 아래 블록을 그대로 복사해 붙여넣으세요.

> ⚠️ 이 폴더의 문서는 전달받은 원본이 인코딩이 깨진 채로 도착해서 내용 기준으로 옮겨 적은 것입니다.

---

```
handoff_D23 폴더를 docs/design/d23/ 에 넣고 커밋해줘.
그다음 D23-BOT-SCENARIO.md 대로 Playwright E2E 를 만들어줘.

세 계정(관리자·광고주·인플루언서)으로 실제 가입해서
협업 하나를 처음부터 완료까지 끌고 가는 거야.

[가장 중요한 원칙 — 1절]
「버튼이 눌렸다」가 아니라 「값이 실제로 바뀌었다」를 확인해줘.
지금까지 찾은 결함이 대부분 그 자리였어 — 모달은 닫히는데 아무것도 안 바뀌는 것들.

  before = KPI 읽기
  하기
  after  = KPI 읽기
  assert(after.미수 === before.미수 - 1)

버튼 존재 여부만 보는 테스트는 그 결함을 못 잡아.

[0절] 실데이터가 아직 없고 테스트 후 전부 지울 거라 격리는 신경 쓰지 마.
  · 이메일 인증·알림톡은 건너뛰어 → Supabase 에서 confirmed 로 시작하거나 환경변수 우회.
    그 두 경로는 내가 따로 확인할 것
  · 사업자번호만 매 실행마다 다른 값으로 (중복 검사가 있어)

이 봇의 목적은 사람이 빼먹는 걸 잡는 거야. 손으로 누르면 어디를 안 봤는지 알 수 없어.
그래서 시나리오를 빠짐없이 순서대로 도는 게 핵심이야.

[2절] 단계 1→9 — 가입부터 평가까지. 각 단계에 확인 항목이 붙어 있어.
  단계 1→9 는 하나의 테스트로 둬. 앞 단계 결과가 뒤 단계 입력이야.
  세 계정은 browser.newContext() 로 각각 → 쿠키가 섞이면 모든 선택이 오작동해.

[3절] 세 화면의 같은 값 — 이게 봇이 가장 잘 잡는 거야
  같은 미수 건을 인플루언서·광고주·관리자에서 읽고 금액·D+ 가 같은지.
  전에 300,000 / 900,000 / 900,000 으로 갈려 있었고,
  「오늘」이 읽을 때마다 달라 D+8 과 D+13 으로 갈렸어.
  사람이 눈으로 하기 가장 힘든 검사야.

[4절] 회귀 — 한 번 고친 게 다시 깨지는지. 일곱 개 다 값 비교로.

[셀렉터] data-testid 를 새로 뿌리지 마.
  화면 문구로 찾는 편이 나아 → 문구가 바뀌면 테스트가 깨지는데,
  그게 잡아야 하는 변경이야.

로컬에서 한 번 끝까지 돌려보고 결과 알려줘. CI 는 그다음이야.
판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
```
