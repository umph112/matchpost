# VS Code 프롬프트 2 (D33) — 셸 판정부터

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
진단이 정확해. 내 문서가 틀렸어 — 폭 클래스는 이미 다 붙어 있었고
원인은 셸이 user-agent 로 가르는 거였어. 폭을 세 번 고쳤는데 세 번 다시 나온 이유가 그거야.

[1번 승인] 셸 판정부터 고쳐줘. 다만 matchMedia 만으로는 부족해.

  ⚠️ 서버에서는 화면 폭을 알 수 없어. matchMedia 로 바꿔도 초기값이 필요하고,
     어느 값으로 시작하든 한쪽은 잘못된 셸을 먼저 그려.
     인플루언서가 'mobile' 로 시작해 PC 에서 깜빡이는 게 정확히 그거야.

  → JS 상태로 가르지 말고 CSS 로 갈라줘.

     지금   const [mode] = useState('mobile')  →  mode === 'pc' ? <PC/> : <Mobile/>
     정답   <PC 셸 className="hidden lg:block">  +  <Mobile 셸 className="lg:hidden">

     두 셸을 모두 렌더하고 미디어쿼리로 한쪽만 보이게 해. 첫 페인트부터 맞아.
     MyCampaignsList 가 이미 그 방식이고 정상 동작해 — 같은 패턴이야.

  · 기준 폭은 1024px(lg)로. 태블릿 세로(768)는 모바일 쪽이 낫아 —
    광고주 PC 셸이 min-w-[1360px] 이라 768 에서는 가로 스크롤이 생겨
  · resize 리스너는 지우면 돼. CSS 가 알아서 해
  · .adv-pc / .inf-pc 클래스는 그대로 둬 — 화면들이 그걸 보고 있어

  ⚠️ 두 셸을 다 렌더하면 안에 든 화면도 두 번 마운트될 수 있어.
     children 을 두 곳에 넣으면 데이터를 두 번 불러오고, 폼 상태가 갈려.
     children 은 한 번만 렌더하고 셸 껍데기(사이드바 · 상단바 · 하단탭)만
     둘로 갈라줘. 그게 안 되는 구조면 멈추고 알려줘 — 방법을 같이 정하자.

[함께 고칠 것 — 관리자 폭]
  관리자는 PC 전용인데 max-w-4xl(896px) · max-w-5xl 로 묶여 있다고 했지.
  표·달력·원장을 보는 화면이라 896px 은 좁아. 제한을 풀어줘 (dashboard 처럼).

[그다음] 두 폭에서 전 화면 캡처해서 커밋해줘.
  PC 1440 · 모바일 390. 봇도 이제 폭으로 갈리니 두 프로젝트로 나눠 돌려줘.

  캡처를 보고 「확인 필요」 6개와 B 5개를 확정할게.
  ⚠️ 추측으로 A 에 넣지 마. 두 폭에서 실제로 재서(getBoundingClientRect) 알려줘.

[미루는 것]
  · A 3개의 군더더기 폭 클래스 — 셸 고친 뒤에 같이 걷어내
  · login·signup 2단 — 공개 화면 B 야. 지금 회색 배경 위 420px 카드인데
    랜딩과 같은 2단으로 바꾸는 건 별건으로 하자. 셸과 무관해
  · terms·privacy 768px — 규격은 720 이지만 48px 차이는 눈에 안 띄어. 그대로 둬

셸 판정만 고치고 캡처까지 해줘. 그 뒤에 범위를 정하자.
```
