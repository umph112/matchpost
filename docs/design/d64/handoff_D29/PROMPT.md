# VS Code 프롬프트 (D29)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D29 폴더를 docs/design/d29/ 에 넣고 커밋해줘.
봇 점검에서 나온 두 건이야. 얽혀 있어서 함께 담았어.

[먼저] 0096 을 Supabase 에서 실행했는지 확인해줘.
  아직이면 그것부터. 화면만 막힌 상태라 두 창이나 빠른 두 번 클릭으로 뚫려.

[2번을 먼저 해줘] 기간 오픈이 안 만들어져
  schedules.date_end 를 읽는 곳은 4곳인데 쓰는 곳이 0곳이야.
  등록 폼에 종료일 칸이 없고, 검색도 date 만 봐.
  D24 「오픈 묶음 보기」가 이 전제 위에 있어 — 「여수 1박 2일」이 그 예였는데
  지금은 그런 오픈을 만들 방법이 없어.

  · 등록 폼 날짜를 두 칸으로 (시작일 · 종료일 선택). 비우면 하루
  · 종료일이 시작일보다 앞이면 막기
  · ⚠️ 중복 검사는 이번엔 시작일만 봐. 기간 겹침은 별도 건이야 —
    지금 막으려 들면 「하루 오픈이 기간 오픈 안에 들어가도 되나」를 정해야 해
  · 검색이 기간을 잡게: 고른 날짜가 [date, date_end] 안에 들면 잡힘.
    ⚠️ date_end 가 null 인 하루 오픈도 잡혀야 해 — or 절에서 빠뜨리기 쉬워
  · 기간 검색일 때는 겹치면 잡아. 「8/25~8/30 찾는데 8/28~8/29 오픈」은 나와야 해
  · 달력에서 기간 오픈은 시작~끝 전체에 표시. 시작일에만 뜨면 중간 날짜를 보다가 놓쳐

[그다음 1번] 오픈 대시로 성사된 협업에 딜시트가 없어
  딜시트가 캠페인 단위로만 조회돼. 오픈 대시로 만들어진 proposals 는
  campaign_id 가 null 이라 어느 캠페인 딜시트에도 안 잡혀.

  · 딜시트의 단위를 협업(proposal)으로 바꿔줘.
    캠페인 딜시트 = 그 캠페인의 proposals 를 모아 보는 것
    오픈 딜시트   = proposal 하나짜리
  · 단계는 캠페인 등록 값이 원본인데 오픈 협업엔 없어 → 기본 9단계를 써
    (lib/campaign-stages.ts 전체). 대시에서 조정하면 그 협업만 줄어들어
  · 새 화면을 만들지 마. 캠페인 딜시트가 proposal_id 로도 열리게 하면 돼.
    /advertiser/deals/[proposalId] 로 라우트를 추가하든 같은 라우트에서 분기하든,
    코드가 덜 갈라지는 쪽으로 판단해서 알려줘
  · ⚠️ 인플루언서 쪽에도 필요해. influencer/proposals 에서 진행 단계를 보는 자리야
  · 들어가는 길은 이미 있는 두 화면에 링크만 —
    인플루언서는 schedule/[id](오픈 묶음), 광고주는 대시 헤더

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
2번 끝나고 커밋한 뒤 1번으로 가줘 — 한 번에 하면 어디가 깨졌는지 알기 어려워.
```
