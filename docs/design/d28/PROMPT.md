# D28 작업 지시

> ⚠️ 이 파일은 전달받은 원본이 인코딩이 깨진 채로 도착해서 내용 기준으로 다시 옮겨 적은 것입니다.
> 문구가 정확해야 하면 원본 `handoff_D28/PROMPT.md` 로 교체해주세요.

`D28-SIGNUP-FIXES.md` 를 읽고 [1]~[4]를 처리해줘.

`handoff_D28` 폴더는 `docs/design/d28/` 에 넣고 커밋해줘.

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
끝나면 5절 grep 과 브라우저 체크리스트 결과를 알려줘.
