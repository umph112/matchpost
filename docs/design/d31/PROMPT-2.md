# VS Code 프롬프트 2 (D31) — 재현 결과 답변

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
재현 잘했어. 내 문서 추측이 틀렸고 네가 찾은 두 줄이 맞아.
push 라서 이력에 남고, /login 이 세션을 안 봐서 폼을 그대로 그리는 것 —
둘이 겹쳐야 증상이 되는 것까지 정확해.

[①② 승인] 둘 다 고쳐줘. 하나만 고치면 증상이 남아.
  · RoleLoginPanel 의 push 4곳 → replace
  · /login · /signup 을 서버에서 확인해 세션 있으면 역할 홈으로 redirect

[③ 「다」로 가줘] 미들웨어에서 /influencer · /advertiser 보호를 빼고
  각 layout.tsx 의 서버 redirect 에 맡겨. 이미 전부 들어 있어 중복이야.

  「가」(고정 세그먼트 나열)는 화면이 늘 때마다 목록을 갱신해야 해 —
  D31 §6 에서 지적한 그 패턴이야. 손으로 붙이는 방식이면 다음에 또 빠져.
  「나」(UUID 판별)는 규칙이 암묵적이라 다음 사람이 몰라.

  ⚠️ 빼기 전에 layout.tsx 의 redirect 가 정말 전부 있는지 확인해줘.
    한 곳이라도 없으면 그 화면이 무보호로 열려.

[탭 구성] 코드대로 두되 하나만 바꿔줘 — 「내 채널」 → 「캠페인 찾기」

    홈 · 오픈 · 대시 · 캠페인 찾기 · 매출

  인플루언서에게 가장 중요한 건 일을 찾는 거야.
  내 채널은 밤 10시 배치라 하루 한 번 갱신되니 매일 볼 화면이 아니고,
  홈 카드에서 들어가면 돼.

  ⚠️ 「더보기」는 넣지 마. 남는 화면을 담는 서랍이 되면
    그 안에 뭐가 있는지 아무도 몰라.

  골격 · prefetch · 상단바 화면 이름은 이 다섯 기준이야.

[공개 프로필 실패] 그대로 둬. 고쳐지면 통과로 바뀌는 감시가 맞아.
  28-mobile-back.spec.ts 커밋해줘.

이거 끝나면 D31 3번(탭 전환 체감) · 4번(저장 표시)으로 가줘.
탭이 확정됐으니 골격도 그 다섯 기준으로 만들면 돼.
```
