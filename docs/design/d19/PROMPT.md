# VS Code 프롬프트 (D19)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D19 폴더를 docs/design/d19/ 에 넣고 커밋해줘.
그다음 D19-EARNINGS-SOURCE.md 대로 해줘.

[먼저 알려둘 것] D17 §2-3·§2-4 를 보류했던 이유를 조사했고, 결론이 바뀌었어.
earnings 는 통합할 대상이 아니라 죽은 테이블이야.

  · sql/migrations/ 에 테이블 정의가 없어 (알림 경로 문자열 2건뿐)
  · insert 하는 코드가 어디에도 없어 (0건)
  · 읽기만 3곳 — dashboard:184,215 · earnings:56

그래서 지금 매출 화면의 요약 카드는 항상 0원이고 목록은 항상 비어 있어.
같은 화면의 미수 카드는 proposals 로 제대로 동작하는데, 인플루언서는
「미수 900,000원」을 보면서 바로 아래 「예정 매출 0원」을 보고 있어.

내가 D7·D17 에서 earnings 를 정산 원본으로 착각하고 지시한 게 오류였어.

[할 것]
1절 — 매출 화면을 proposals 기반으로 다시
  · 상태 정의 4개 (예정 / 미수 / 확인 대기 / 완료). 지금 STATUS_FILTERS 는
    earnings.status 문자열이라 무관해. 「진행중」과 「완료」·「결제완료」가
    무엇이 다른지 화면에서 구분되지 않던 문제(D7 §5-3)도 이걸로 같이 풀려
  · 미수 판정은 earnings/page.tsx:80–130 로직을 그대로 재사용해.
    같은 기준이어야 세 화면(인플루언서·광고주·관리자)의 미수가 일치해
  · 요약에 미수 칸을 추가해. 미수가 카드로는 떠 있는데 요약에는 없어
  · 목록 주 라인은 브랜드, 부제는 캠페인 제목
  · 「카테고리별 수입」은 없애거나 브랜드별로 바꿔. 비어 있는 절을 남기지 마
  · CSV 헤더도 새 필드로

2절 — 홈 화면도 같이 (dashboard:184,215)
  「이번 달 매출」이 항상 0원이야. 같은 proposals 쿼리로 바꿔줘

2-5절 — 직접 등록한 일 (먼저 확인만 해줘)
  인플루언서는 매치포스트 밖 일정도 자기 달력에 적어 (「내 일정」 · 점선 = 직접 등록).
  전체 일정 관리를 매포에서 하게 하려는 거고, 두 종류가 섞여 있어 —
  밖에서 생긴 돈 버는 일(협찬·외주)과 그냥 개인 일정(병원·여행).
  · 직접 등록 건이 어디에 저장되고 금액 필드가 있는지 먼저 확인해줘 (grep 은 문서에)
  · 금액이 적힌 건만 매출에 합산하고 「직접 등록」 배지로 구분,
    요약에 「매치포스트 N원 · 직접 등록 N원」을 나란히
  · ⚠️ 금액은 선택 필드여야 해. 필수로 두면 병원·여행을 못 적어
  · 금액 필드가 없으면 이번엔 proposals 만 하고 알려줘
  · ⚠️ 합산하더라도 테이블을 하나로 합치지 마. 매치포스트 건은 우리가 아는 사실이고
    직접 등록은 본인이 적은 거라 성격이 달라. 화면에서 나란히 두는 것으로 충분해
  · ⚠️ 미수는 매치포스트 건에만. 직접 등록 건의 미수는 우리가 알 수 없어

3절 — earnings 테이블은 남겨둬
  코드 참조만 없애. 테이블 정의가 마이그레이션에 없으니 손으로 만든 걸 수도 있고,
  행이 있으면 되돌릴 수 없어. 확인 후 별도 마이그레이션으로 지우자.
  이번 회차에 drop 하지 마.

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
끝나면 4절 grep 과 브라우저 체크리스트 결과를 알려줘.
```
