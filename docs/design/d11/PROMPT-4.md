# VS Code 프롬프트 4 — 검색 페이지 2·3단계 (한 번에)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D11_diff 폴더를 docs/design/d11/ 에 넣고 커밋해줘.
검색 페이지 남은 두 단계를 순서대로 해줘. 문서는 각각이야.

[먼저] D11-STAGE2-CALENDAR.md → 7열 달력
  1단계에서 만든 필터 카드의 <input type="date"> 를 달력으로 바꾸는 거야.
  · 날짜별 오픈 집계 쿼리부터 (문서 1절). 가상값도 필요 없고 실제 오픈만.
  · 격자 repeat(7,1fr) gap:3px · 폭 388px · 앞 빈칸은 1일의 요일 수만큼
  · 칸 상태 4가지 우선순위 (문서 3절) → 오픈 있는 날에만 색이 붙어
  · 특정일/기간 두 모드, 기간은 두 번 눌러야 완성 (문서 4절)
  · 안내 문구가 세 번 바뀌어 → 지금 무엇을 눌러야 하는지 알려주는 거야
  · 우측에 선택 요약 카드 + 빠른 선택 칩 (문서 5절)

[그다음] D11-STAGE3-CONNECTIONS.md → 친구등록
  달력이 자리를 잡은 다음 해줘. 카드가 필터 아래에 들어가거든.

  정책 (문서 1절) → 세 경로로 맺어져:
    ① invite  캠페인 링크 초대로 참여 → 자동
    ② collab  협업 완료 → 자동
    ③ manual  광고주가 검색·프로필에서 직접 → 인플루언서에게 알림, 상대 승낙 불필요

  중요한 두 가지:
  · 친구등록은 대시 권한이 아니야. 등록해도 첫 대시는 양쪽 승낙을 거쳐.
    등록이 곧 연락 허가가 되면 무단 접이 통로가 돼.
  · 인플루언서가 해제해도 광고주에게 알리지 마. 알리면 해제를 못 해.

  구현:
  · advertiser_connections 테이블 신설 (문서 2절의 DDL 원문)
    unique (advertiser_id, influencer_id) · 이미 있으면 source 를 덮지 마.
    먼저 맺어진 경로가 참이어야.
  · 자동 등록 3곳: campaign/join · deal/complete · connections(신규 POST)
    upsert … on conflict do nothing
  · 검색 페이지 친구등록 섹션 (문서 3절) → 필터 아래, 결과 위. 4장. 없으면 섹션 자체를 안 그리게
  · 「친구등록」 버튼 (문서 4절) → 누르면 즉시 「친구등록됨」. 모달 띄우지 마, 되돌리기 쉬운 행동이어야
  · /advertiser/connections 의 경로 컬럼 추가 (문서 5절) → 7열로 늘어남
  · 인플루언서 알림 + 설정에서 해제 (문서 6절)

  경로 배지 색:
    invite 초대  #DBEAFE / #1D4ED8
    collab 협업  #DCFCE7 / #15803D
    manual 직접   #F1F1F4 / #7C7C88
  어떻게 아는 사이인지가 다음 대화의 문구를 정해.
  초대하는 사람에게 처음 만난 것처럼 말을 걸면 안 되엇.

판단이 필요하거나 문서의 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.

되면 두 문서의 확인 전 grep 과 체크리스트 결과를 알려줘.
```

---

## 순서를 지켜야 하는 이유

3단계 친구등록 카드가 **필터 카드 아래**에 들어갑니다. 달력이 먼저 들어가면 그 자리가 확정되고,
반대로 하면 나중에 배치를 다시 잡아야 합니다.

## 문서 목록

| 파일 | 단계 |
| --- | --- |
| `D11-STAGE2-CALENDAR.md` | 2단계 → 달력 |
| `D11-STAGE3-CONNECTIONS.md` | 3단계 → 친구등록 |
| `D9-PC-LAYOUT-SPEC.md` | 값이 더 필요할 때 |
| `advertiser-mypage-pc.dc.html` | 프로필 표준 (브라우저에서 바로 열림) |
