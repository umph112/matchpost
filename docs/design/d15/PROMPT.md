# VS Code 프롬프트 (D15)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D15 폴더를 docs/design/d15/ 에 넣고 커밋해줘 (.dc.html 과 support.js 도 함께 —
지난번에 목업이 두 번 빠졌어).

그다음 D15-PROFILE-EDIT.md 를 해줘. 하나짜리 작업이야.

[문제] 읽는 쪽만 있고 쓰는 쪽이 없어.
  /advertiser/[id] 는 one_line · intro · brands · history 를 다 렌더하는데(110·245·255행),
  팀 페이지 「공개 프로필」 절에는 마케팅 연락처만 있어.
  컬럼(0082)도 있고 표시도 되는데 광고주가 자기 소개를 쓸 수 없는 상태야.
  그래서 그 카드들이 영원히 안 보여.

[할 것] 팀 페이지 「공개 프로필」 절에 필드 4개를 이어붙여
  한 줄 소개(input) · 소개 · 함께 일하는 브랜드 · 주요 이력(textarea 3개)

  · 새 페이지를 만들지 마. 마케팅 연락처와 같은 자리에 있어야
    「인플루언서가 보는 화면」이 한 곳에서 관리돼
  · 저장은 기존 persistContact 와 같은 방식으로 onBlur. 저장 버튼 만들지 마
  · 빈 문자열은 null 로 저장해. 읽는 쪽의 ap?.brands && 조건이 그대로 동작해야 해
  · 브랜드·이력은 줄바꿈을 그대로 저장해. 읽는 쪽이 whitespace-pre-wrap 이라
    파싱할 필요가 없어
  · 한 줄 소개가 비면 그 아래 안내 —
    「지금은 소개의 첫 문장이 쓰여요 — "{intro 첫 문장}"」
    읽는 쪽에 이미 그 폴백이 있으니(110행) 로직을 새로 만들지 말고 말해주기만 해

[함께 확인] 프로필 페이지의 소개·브랜드·이력 카드에 「직접 작성」 배지가 있는지.
  없으면 추가해줘 (10px/700 · #F1F1F4 · #7C7C88).
  「매치포스트 기록」 4카드에는 붙이지 마 — 거기엔 「광고주가 고칠 수 없어요」가 있어.
  기록과 자기 어필이 섞이면 기록 4카드 전체를 못 믿게 돼.

문서 5절의 안내 문장도 넣어줘 — 1인 대행사가 「무엇을 적어야 하나」로 멈추지 않게 하는 거야.

DB 마이그레이션은 필요 없어. 0082 컬럼을 그대로 써.

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
끝나면 6절 grep 과 체크리스트 결과를 알려줘.
```
