# VS Code 프롬프트 2 (D24) — 판단 6건 답변

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
사전 검증 정확해. 여섯 다 그대로 가. 두 곳만 덧붙일게.

[승인] 추가로 넣을 것 — RLS + set_proposal_perk()
  둘 다 맞는 판단이야. 새 테이블을 RLS 없이 두면 남의 일정이 보여.
  proposals UPDATE RLS 를 확인할 수 없으면 security definer 함수가 맞아 (0087 패턴).

[① 제목] 그대로. 다만 부제가 길면 「1박 2일」은 빼줘 — 날짜를 보면 알아.
  「8월 28일 – 29일 · 협업 3건 확정 · 방문 4곳」 정도면 충분해.

[② 표시 대상] 합계는 양쪽 확정만 — 맞아. 다만 협의중 건을 아예 숨기지는 마.
  「그 오픈에 대시가 왔는데 화면에 없다」가 되거든.

  시간표 아래에 노란 카드로 따로 둬:
    헤더 「아직 시간이 안 정해진 N건」 background:#FFFBEB · border:1px solid #FDE68A
         우측 「시간을 정해야 시간표에 들어가요」 11.5px · #B45309
    행   브랜드 + 「협의중」 배지(#FEF3C7/#B45309) + 금액 + 「대시에서 시간 정하기」(검정 버튼)
    0건이면 카드 자체를 렌더하지 마

  합계·빈 시간 계산에는 넣지 마. 시간이 없으니 넣을 자리가 없어.

[③ stops 없으면 start_at 파생] 맞아. 그게 실데이터 경로야.

[④ 「오픈 수정」 버튼] 빼는 게 맞아. 갈 곳 없는 버튼은 없느니만 못해.

[⑤ 「여기 열어두기」] 오픈 등록으로 보내되 **날짜와 시간대를 프리필**해줘.
  /influencer/schedule?date=2026-08-28&from=13:00&to=18:00 처럼.
  빈 폼으로 보내면 방금 본 걸 다시 입력해야 해.
  등록 페이지가 그 쿼리를 못 읽으면, 읽게 하는 것까지가 이번 범위야.

[⑥ 캘린더 진입점] 안 건드리는 게 맞아. /day/[date] 는 광고주와 공유하는 화면이야.

[추가] docs/design/d21 · d22 폴더도 같이 넣어줘. 지금 없더라.

작업 계획대로 진행해줘. 0090 은 Supabase 에서 먼저 실행하고,
커밋·push 는 빌드 결과 보고 뒤에 따로 지시할게.
```
