# VS Code 프롬프트 (D32)

D30 [2] 다섯 시나리오가 끝난 뒤 전달하세요.

---

```
handoff_D32 폴더를 docs/design/d32/ 에 넣고 커밋해줘.

이건 테스트가 아니라 제품 구멍이야. findings 4번 「캠페인 협업 — 만들 수가 없음」의 본체야.
캠페인을 등록할 수 있고 검색에 보이는데 아무도 참여할 수 없어 —
광고주의 주력 상품이 성립하지 않는 상태야.

[좋은 소식] 그릇은 다 있어. 값을 넣는 곳만 없어.
  ConfirmDashModal 이 campaignId 를 받으면 p_kind:'campaign' 으로 넘기고,
  DashSendButton 도 그대로 전달해. 그런데 이 값을 채우는 화면이 하나도 없어.
  proposals 를 만드는 네 곳이 전부 p_campaign_id: null 로 하드코딩돼 있어.

[1절 지원] 인플루언서 검색 카드에 「지원하기」
  · 캠페인 카드는 「지원하기」, 인플루언서 오픈은 「대시 보내기」 —
    캠페인은 조건이 이미 정해져 있고, 오픈은 조건을 제안하는 거라 성격이 달라
  · ConfirmDashModal 을 재사용하되 날짜를 캠페인 기간 안으로 제한해.
    지금 그 모달엔 날짜 제한이 없어
  · ⚠️ 크레딧을 깎지 마. 크레딧은 광고주가 대시 보낼 때 쓰는 값이야
  · 중복 지원 막기 — 화면과 DB 양쪽. 0096 처럼 화면만 막으면 두 창으로 뚫려

[2절 확정] 광고주 캠페인 상세에 지원자 목록
  · 딜시트 위에 지원자 카드. 확정하면 그 사람이 딜시트 행으로 내려가
  · ⚠️ 지원은 인플루언서가 이미 「하겠다」고 한 거야. 그래서 광고주 확정 하나로 양쪽 성립.
    「지원 → 확정」과 「대시 → 수락」이 같은 두 칸을 다른 순서로 채워
  · ⚠️ 모집 인원 도달해도 자동으로 닫지 마. 5명 모집인데 6명째가 더 좋을 수도 있어.
    「N명 확정 · 모집 인원 도달」만 알려주고 광고주가 마감을 누르게 해
  · 반려 사유는 선택이야. 필수로 하면 지원자 많을 때 아무도 반려를 안 눌러

[3절 딜시트 단계 — 따로 커밋해줘]
  computeEnabledStages 에 「가이드」가 없는 걸 네가 확인했잖아.
  ALL_STAGES 에도 STAGE_OWNER 에도 있는데 그 함수만 빠뜨려.

  ⚠️ 진행 중인 건의 단계 배열이 바뀌어. 보정 없이 함수만 고치면
    「원고」에 있던 건이 「방문」으로 되돌아가.

  두 길 중 어느 쪽이 안전한지 보고 먼저 알려줘:
    ① 함수를 고치고 진행 중인 건의 단계 인덱스를 보정
    ② 캠페인에 단계 배열을 저장해두고 기존 건은 그대로 · 신규만 9단계

  확인은 「수정/컨펌 끄기」로 만든 캠페인의 딜시트가 그 단계 없이 열리는지야.
  그게 「등록 때 정한 값이 원본」의 증거야.

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
1절 → 2절 순서로 하고, 3절은 따로 커밋해줘.
```
