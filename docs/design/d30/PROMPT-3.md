# VS Code 프롬프트 3 (D30) — 봇 고정값 정리

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
A~D 전부 승인. B 도 고쳐줘 — 404 인 채로 두면 11개 중 1개가 계속 판정 불가야.

[가장 중요한 것] 25-deal-progress 4건이 「생략」으로 ok 찍힌 게 이번 실행의 최대 문제야.

  D30 §2 회귀 대상 중 두 개(서버액션 3개 · 인플루언서 단계 이동)가 검증 0 인데
  로그만 보면 통과로 읽혀. 조용한 실패를 잡으려고 봇을 돌리는데 봇이 조용히 실패했어.

  · 전제가 없어서 못 돌린 건 ok 로 찍지 마. skip 이나 fail 로 남겨줘.
    스위트 요약에 「미검증 N건」이 보여야 해
  · A~C 고친 뒤 새 RUN 으로 전체 재실행해서 이 4건이 실제로 도는 걸 확인해줘.
    그게 끝나야 새 시나리오 5종을 쓰는 의미가 있어

[A] input[type=date] → .first() — 맞아. D29 에서 종료일 칸이 생겼어

[B] 하드코딩 SCHEDULE_ID → 실행 시점에 DB 에서 찾기
  ⚠️ 다른 스펙에도 같은 패턴이 있는지 함께 봐줘.
     지난 RUN 의 id 를 박아둔 곳이 또 있으면 다음에 같은 실패가 나

[C] shot() 으로 교체 — 맞아. tests/screenshots 는 커밋이 안 돼
[D] .gitignore 에 tests/.logs/ — 맞아

[발견 1건] /admin/users 문구도 고쳐줘
  「승인하면 가입 축하금 20,000C가 지급됩니다」 → 사실과 달라.
  원장을 보면 가입 시점(api/signup)에 이미 지급돼 있어. 승인은 지급과 무관해.

  「승인하면 캠페인을 열 수 있습니다」 정도로. 가입 축하금은 언급하지 마 —
  이미 받은 것을 앞으로 받을 것처럼 쓰면 관리자가 잘못 판단해.

판단이 필요하거나 또 어긋나면 멈추고 물어봐.
A~D 고치고 재실행한 결과를 알려줘. 새 시나리오는 그다음이야.
```
