# VS Code 프롬프트 (D30)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D30 폴더를 docs/design/d30/ 에 넣고 커밋해줘.
세 건이야. 1 → 2 → 3 순서로 해줘 — 1이 작고 봇이 그 결과까지 확인할 수 있어.

[1] 방문 로그에 시각 남기기
  user_visit_log 가 날짜 단위라 관리자 「오늘」 트래픽을 시간대별로 못 그려.
  · visited_at timestamptz 추가 + 인덱스. 기록할 때 now() 를 함께 남겨
  · ⚠️ visited_on 은 그대로 둬 — visit-weekly · visit-monthly 가 그걸 읽어
  · ⚠️ 과거 행은 백필하지 마. 없는 시각을 지어내는 거야
  · 관리자 「오늘」 트래픽에 시간대 탭 추가. 일별 보기는 없애지 말고 탭으로 둘 다
  · 데이터 없는 구간은 「이 날짜 이전은 시간 정보가 없어요」로 비워

[2] 봇 시나리오 재실행
  D23 이후 고친 게 많아. 회귀 확인이야.
  · headless: false 로, 시나리오마다 스크린샷을 docs/design/d30/screens/ 에 커밋
  · 막히면 우회하지 말고 멈춰. 사람도 같은 곳에서 막혀

  이번에 확인할 자리 (그동안 고친 것):
    인플루언서 셸 11개 화면 사이드바 / 서버액션 3개가 DB 에 남는지 /
    오픈 중복 방지 / 기간 오픈 중간 날짜 검색 / 오픈 협업 딜시트 양쪽 /
    인플루언서 단계 이동(방문·원고·게재) / 「대시 보내기」 라벨 / 버튼 두 줄 접힘

  아직 봇이 안 지나간 자리 (새로):
    팀 운영 — 초대 → 가입 → 담당 → 휴무 신청 → 대표 수락 + 대행자
    이관 — 퇴사 예정 → 메모와 함께 이관 → 받는 사람 확인
    관리자 — 「오늘」 큐 숫자 = 실제 건수 · 신고 종결 · 크레딧 지급
    크레딧 — 원장에 남는지 · ⚠️ 잔액 = 원장 합 (어긋나면 돈 문제야)
    공개 페이지 — /campaigns/[id] · /opens/[id] · /advertiser/[id] 로그아웃 상태

[3] 관리자 광고관리 — 이번엔 하지 마
  화면 프로토타입이 아직 없어. 스펙만으로 만들면 값(그리드·색·간격)을 지어내게 돼.
  내가 목업을 만들어서 별도로 보낼게. 문서의 3절은 참고용으로만 둬.

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
1 끝나고 커밋한 뒤 2로 가줘.
```
