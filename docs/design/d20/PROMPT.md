# VS Code 프롬프트 (D20)

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D20 폴더를 docs/design/d20/ 에 넣고 커밋해줘.
그다음 D20-SETTLEMENT-DATE-CHANGE.md 대로 해줘.

D19 §1-3-2 에서 조사만 하고 미룬 건이야. 지금 가장 급해 —
「결제를 9/5로 미루자」고 합의해도 값이 안 바뀌어서
8/21부터 미수로 뜨고 광고주에게 지연 알림이 매일 가고,
그 횟수가 제재 판정 근거로 쌓여. 합의를 지킨 광고주가 제재 근거를 쌓는 거야.

[순서]

2절 — 스키마 (0088)
  proposals.settlement_date (사람별 합의) + proposed_date_kind ('progress'|'settlement')
  · 기존 행은 건드리지 마. kind 가 null 이면 진행일로 읽어 — 지금까지 쓰인 방식이야
  · ⚠️ lib/deals/settlementDate.ts 를 만들어서 매출 시점을 한 곳에서 읽게 해줘.
    campaigns.settlement_date 를 직접 읽는 곳이 하나라도 남으면 그 화면만 옛 날짜를 봐.
    특히 api/admin/batch (지연 알림) 가 중요해 — 거기가 안 바뀌면 알림이 계속 가

3절 — 제안 · 수락
  · 새 화면 만들지 마. 대시 대화창에서 해 (양쪽 다 제안 가능)
  · 카드에 종류 라벨 — 「진행일 제안」 / 「결제일 제안」.
    지금은 proposed_date 만 보고 그려서 같은 모양이 두 뜻으로 쓰이게 돼
  · ⚠️ 결제일 제안 카드에 「오픈해두신 날짜」 부제를 붙이지 마. 오픈과 무관해
  · accept_date_proposal 을 종류로 갈라줘. 기존 함수를 고치지 말고 안에서 분기하거나
    새로 만들어 — 진행일 수락이 깨지면 안 돼
  · 결제일 수락은 start_at 을 건드리지 않고, 겹침 검사도 안 해 (진행일만 겹치면 안 됨)
  · ⚠️ 지연 알림 카운터를 0으로 리셋해줘. 합의된 변경은 지연이 아니야.
    리셋 안 하면 이미 쌓인 횟수가 제재 근거로 남아

4절 — 딜시트·정산에 「(변경)」 파란 표시 (D9 §3 규칙 그대로)

5절 — 광고주 미수 카드 문구를 버튼으로
  지금 「대시에서 예정일을 다시 정해주세요」라고만 하고 갈 길이 없어.
  그 자리에서 3-1 모달을 열게 해줘

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
절 단위로 끊어서 진행하고 각 절 끝에 보고해줘.
0088 SQL 은 내가 Supabase 에서 실행할게.
```
