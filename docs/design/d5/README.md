# 매치포스트 트랜스퍼 **D5 노트** (2026-08-05)

> 1~4차는 이미 넘겼고 레포에 반영됐습니다(마이그레이션 `0034`~`0057` 확인).
> **이 폴더는 그 다음 바뀐 것만 담았습니다 — 전부 합쳐 20KB 남짓.**
> 큰 원본 문서(`MANUAL-SOURCE.md` 663KB · `SPEC-SCREENS.md` 138KB · 프로토타입 15종)는
> **당기지 마세요.** 컨텍스트만 먹고 이번 작업엔 필요하지 않습니다.

## 파일 3개

| 파일 | 무엇 | 크기 |
| --- | --- | --- |
| `IMPLEMENT-5-DELTA.md` | **이번에 할 것 전부** — A 대시 상태 규칙 · B 정산 규칙 · C 잡은 확정 | ~2KB |
| `DECISIONS-D5.md` | 그 규칙들이 왜 그런지 (결정 기록 추가분 12줄) | ~3KB |
| `TEST-D5.md` | 프로토타입에서 무엇이 이겼고 어떻게 고쳤는지 | ~5KB |

## 두는 곳 · 첫 프롬프트

```
docs/design/d5/  ← 이 폴더 3개 파일
```

```
docs/design/d5/IMPLEMENT-5-DELTA.md 를 읽고 A절(대시 상태 규칙)만 구현해줘.
근거가 필요하면 같은 폴더 DECISIONS-D5.md 를 봐.
스펙과 다르게 가야 할 이유가 생기면 멈추고 물어봐줘.
```

A → B → C 순서로 **절 단위로 끊어서** 주세요. 한 번에 다 주면 이유를 다 읽고 갑니다.

## 눈으로 확인할 화면이 필요할 때

프로토타입은 레포에 넣지 말고 **브라우저로만 여세요** — 트랜스퍼 폴더의
`advertiser-mypage-pc.dc.html` · `influencer-mypage-pc.dc.html` (같은 폴더 `support.js` 필요).
대시 규칙은 그 두 파일의 "대시" 화면에 그대로 들어 있습니다.

## B에 손 대실 때만 추가로 필요한 것

광고주 정산 화면은 레포에 아직 없습니다. 그럼 `SPEC-SCREENS.md` 의 **정산 절만 잘라서** 주세요
(전체 138KB를 다 주지 말 것).
