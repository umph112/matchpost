# VS Code 프롬프트 (D26) — 관리자 셸 + 「오늘」

아래 블록을 그대로 복사해 붙여넣으세요.

---

```
handoff_D26 폴더를 docs/design/d26/ 에 넣고 커밋해줘.
⚠️ admin-pc.dc.html 과 support.js 도 함께 — 문서만 옮기면 값을 잴 수 없어.

[먼저 알려둘 것]
관리자 구현 지시서를 D9 이후 한 번도 만들지 않았어. 내 누락이야.
그래서 지금 관리자는 화면 6개가 「← 뒤로」로만 이어져 있고 사이드바가 없어.
프로토타입에는 12개 화면이 사이드바에 걸려 있어.

D26-ADMIN-ACTIONS.md 에 관리자 액션 28개를 전수 조사해 레포와 대조했어.
처리 경로가 있는 게 9개, 없는 게 19개야. 그리고 자동 배치가 13개 있는데
관리자 화면에 하나도 없어 (6-1절).

[이번 회차 = 9절 셸 + 「오늘」만]
나머지 화면은 순서대로 따로 할게. 셸이 먼저 서야 거기에 걸 수 있어.

1) src/app/admin/layout.tsx — 셸
   · 사이드바 236px · sticky · 12항목 4그룹 (9-2 에 목록·스타일)
   · 본문 padding:20px 28px 40px · gap:14px
   · ⚠️ 간격은 14px 하나야. 20·18·16 섞지 마 (CLAUDE.md 고정 규칙)
   · 배지 숫자를 하드코딩하지 마. 각 화면 원본에서 파생시켜

2) 「오늘」 — 처리 대기 큐 4칸 (9-3)
   미입금 신고 · 사업자 확인 · 결제 지연 · 시스템 현황
   · 네 번째 칸은 건수가 아니라 시스템 현황이야 —
     배치 13개 중 실패한 게 여기 떠. 장애를 문의로 알게 되면 늦어
   · 급한 카드는 붉은 테두리 + 배경
   · 결제 지연은 settlementDateOf() 를 써. 세 화면(인플·광고주·관리자)의 미수가 같아야 해

3) 「오늘」 나머지 절 (9-4) — 회원 현황 · 트래픽 · 오늘 오픈 · 오늘 할 일 · 신고 목록
   좌우 컬럼 바닥선을 맞춰 (각 컬럼 flex-column · 늘어날 카드 flex:1 min-height:0)

4) 이미 있는 화면 5개는 셸 안으로 옮기기만 (9-5)
   reports · credits · credits/policy · users · sanctions
   · 다시 만들지 마. 「← 뒤로」만 빼
   · ⚠️ sanctions 는 프로토타입에서 신고 화면 안에 있어.
     사이드바에 따로 두지 말고 신고 상세에서 들어가게 해

[정책 확정분 — 이번 회차에 반영할 것]
  · 관리자는 대화·캠페인·딜시트·오픈을 상시 열람할 수 있어. 읽기만 하고 글은 안 써
  · trust_score 는 관리자만 봐 — 제재 판정에 쓰고 회원 화면엔 노출 안 함
  · 휴면 60일 — 53일 예고 → 60일 전환 → 1회는 로그인으로 자동 해제 →
    2회부터 관리자 확인. 「오늘」 큐에 「휴면 해제 요청」이 필요해 (7-1절)
  · 진행 중인 협업이 있는 계정은 휴면으로 바꾸지 마. 딜시트가 살아 있으면 정산이 막혀

판단이 필요하거나 문서와 실제 코드가 어긋나면 추측하지 말고 멈춰서 물어봐.
끝나면 10절 grep 과 체크리스트 결과를 알려줘.
```
